# iconocracy-corpus-sdk CLI 1.0.0

Welcome to the iconocracy-corpus-sdk CLI documentation. This guide will help you get started with installing and using the iconocracy-corpus-sdk CLI.

This CLI was generated by [Postman](https://www.postman.com)

## Versions

- CLI version: `1.0.0`

## Table of Contents

- [Setup & Configuration](#setup--configuration)
  - [Supported Language Versions](#supported-language-versions)
  - [Installation](#installation)
- [Available Commands](#available-commands)
- [Agent Skills](#agent-skills)

# Setup & Configuration

## Supported Language Versions

This CLI requires `Go >= 1.19.0` to be installed on your system.

## Installation

To build and install the CLI, run the following commands from the root of the project:

```bash
go mod tidy
go build .
```

This will produce an executable binary in the current directory. You can then run it directly:

```bash
./iconocracy-corpus-sdk --help
```

To make the CLI available globally, move the binary to a directory in your `PATH`:

```bash
mv ./iconocracy-corpus-sdk /usr/local/bin/iconocracy-corpus-sdk
```

## Available Commands

The CLI exposes the following top-level command groups. Each group contains subcommands that map to API operations.

Use the `--help` flag on any command or subcommand to see available options:

```bash
iconocracy-corpus-sdk <command-group> --help
iconocracy-corpus-sdk <command-group> <subcommand> --help
```

For example:

```bash
iconocracy-corpus-sdk get --help
iconocracy-corpus-sdk get get-data --help
```

### `get`

| Subcommand | Description                                                                                                                                                                                                                                                                                                                                                                                 |
| :--------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `get-data` | This is a GET request and it is used to "get" data from an endpoint. There is no request body for a GET request, but you can use query parameters to help specify the resource you want data on (e.g., in this request, we have `id=1`). A successful GET response will have a `200 OK` status, and should include some kind of response body - for example, HTML web content or JSON data. |

### `implemented-api`

| Subcommand               | Description |
| :----------------------- | :---------- |
| `get-diary`              |             |
| `get-scout-data`         |             |
| `get-corpus-stats`       |             |
| `get-corpus-countries`   |             |
| `search-corpus-analysis` |             |
| `list-corpus-analysis`   |             |
| `list-corpus-items`      |             |
| `get-corpus-item`        |             |

### `post`

| Subcommand  | Description                                                                                                                                                                                                                               |
| :---------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `post-data` | This is a POST request, submitting data to an API via the request body. This request submits JSON data, and the data is reflected in the response. A successful POST request typically returns a `200 OK` or `201 Created` response code. |

## Agent Skills

This CLI includes pre-built **skills** — structured, machine-readable documents that describe how to invoke each command. Skills are designed for use with AI agents and coding assistants (e.g., Claude, Cursor, Copilot) to enable accurate, context-aware command generation without hallucination.

Each skill describes a specific CLI command in detail: its purpose, required and optional parameters, expected inputs, and usage examples. When an AI agent loads a skill before invoking a command, it gains the full context needed to construct correct invocations on the first attempt.

Skills are organized by command group at `.claude/skills/<command-group>/SKILL.md`, with individual subcommand skills at `.claude/skills/<command-group>/<subcommand>/SKILL.md`.
