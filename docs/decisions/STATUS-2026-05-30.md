# Status consolidado — 2026-05-30

Saída da sessão de integridade GitHub + decisões pendentes. Organizado por: (✅) feito,
(⏳) decisão tua, (🧹) limpeza pós-aprovação.

---

## ✅ Concluído (verificado, nada pushado — tudo local)

### Repositórios
| Repo | Antes | Agora |
|---|---|---|
| **iconocracy-corpus** (prioridade) | 0/0, mas 5 worktrees órfãos | **0/0 limpo**; 2 worktrees (main + `research/permanent` by-design) |
| **Research** (não-prioridade) | 5↓/1↑ divergido, 31 worktrees | **0↓/1↑** rebaseado; **1 worktree** (só main) |

### Worktrees
- **31 removidos** no total (13 SAFE perda-zero + 18 sandboxes Research). Princípio: preservar-por-construção.
- Commits de codex seguros (`refs_containing ≥9`); branches `claude/*` preservadas.

### Documentação
- `CLAUDE.md` (iconocracy) → *Known Data Issues #3* estendido com o drift N=165 (ver ⏳-1).

---

## ⏳ Decisões tuas (não-faxina — exigem tua chamada)

### 1. Corpus — **REFORMULADO pela dialética + santa-loop** (era "165 vs 265")
Ver traço completo: `docs/decisions/DIALETICA-N165-vs-265.md`.
- A pergunta "165 vs 265" era **falso enquadramento**. Verificação adversarial (Opus+Gemini, ambos FAIL)
  revelou: o corpus é codificado por **6 instrumentos** (`coded_by`), e **41 itens são não-codificados**
  → **265 nunca foi um N analítico válido**.
- Eixo real: **estrato de validade × proveniência de instrumento**, não data. N reais: **≈145** (só
  IconoCode), **≈223** (todos codificados, após auditoria), 265 inválido.
- **Ação sequenciada (não é escolha A/B):** estratificar por `coded_by` → quarentenar os 41 não-codificados
  → confiabilidade inter-instrumento (opus vs opus-4.6) → auditar import/migration → definir N analítico por
  validade → dataset card no Cap.2 trocando "N=165" por "N=[válido]".
- Manuscrito afirma 165: `Cap2_metodologia:66,161` · `Introducao_rev:127,193` · `Cap3` · `notas/Cap3_outline`.

### 2. Branch `infra/hub-consistency-refactor` (iconocracy-corpus)
- **10 commits não-merged** (17/abr): "promote scout batch into canonical ledger", repair/multiplicity-aware vault sync.
- **Opções:** mergear · cherry-pick parcial · descartar branch.

### 3. 44 notas de candidato SCOUT (salvas)
- BR-011…BR-054 + `gallery_ingest.py`, ausentes do main, recuperadas de `pr-13` em
  `~/Research/.worktree-salvage-20260530/pr-13-candidatos/`.
- **Opções:** revisar e mover p/ `vault/candidatos/` · descartar.

---

## 🧹 Limpeza pós-aprovação (quando conferires a árvore do Research)
- `git -C ~/Research stash drop` + `git -C ~/Research branch -D backup-pre-rebase-20260530`
  (stash está **parcialmente aplicado** — NÃO re-aplicar).
- `.learnings/LEARNINGS.md` (315L) preservado untracked após o flatten upstream removê-lo
  do versionamento → re-trackear (convenção per-repo) ou honrar a remoção?
- Push do Research (1 commit `cronjob orchestration`) quando autorizares.

---

## 📁 Rede de segurança (nada perdido)
- Branch `backup-pre-rebase-20260530` · stash `pre-rebase dirty` · branch `infra/hub-consistency-refactor` (10 commits).
- `~/Research/.worktree-salvage-20260530/` — 58 arquivos + README explicativo.
