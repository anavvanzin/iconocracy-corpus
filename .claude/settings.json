{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_FILE_PATH\" | grep -q vault/candidatos/SCOUT- && echo \"Vault note saved: $(basename \"$CLAUDE_FILE_PATH\")\" || true",
            "statusMessage": "Tracking vault writes..."
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_FILE_PATH\" | grep -q corpus/corpus-data.json && echo \"corpus-data.json modified -- run sync_companion.py to update companion\" || true",
            "statusMessage": "Checking corpus sync..."
          }
        ]
      }
    ]
  }
}
